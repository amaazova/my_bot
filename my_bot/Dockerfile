# Указываем базовый образ
FROM python:3.9-slim

# Директория в контейнере, где будут храниться файлы
WORKDIR /app

# Копируем всё из текущей папки (.) на локальном компьютере в /app в контейнере
COPY . /app

# Устанавливаем зависимости
RUN pip install --no-cache-dir -r requirements.txt

# По умолчанию при запуске контейнера выполняется команда:
CMD ["python", "bot.py"]

